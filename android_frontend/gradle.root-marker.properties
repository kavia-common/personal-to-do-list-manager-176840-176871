# Marker file to help analyzers identify this directory as a Gradle project root.
# Actual configuration uses settings.gradle.dcl (Gradle Declarative DSL).
# Primary app module: :app
# Build with: ./gradlew :app:assembleDebug
